import { postAylienData } from '../client/js/formHandler'
import "regenerator-runtime/runtime.js";

describe('Test "postAylienData()" should exist' , () => {
  test('It should return true', async () => {
    expect(postAylienData).toBeDefined();
  });
});

